dependencies:
  override:
    - echo '{:user {:plugins [[lein-voom "0.1.0-20150822_000839-g763d315"]]}}' > ~/.lein/profiles.clj
    - lein voom build-deps
    - ./start_datomic.sh

test:
  override:
    - lein with-profile dev,circle-ci test :default:
        timeout: 480

machine:
  java:
    version: oraclejdk8
  services:
    - docker
  pre:
#    - wget --no-check-certificate --http-user=${DATOMIC_USERNAME} --http-password=${DATOMIC_PASSWORD} https://my.datomic.com/repo/com/datomic/datomic-pro/0.9.5350/datomic-pro-0.9.5350.zip -O datomic-pro-0.9.5350.zip
#    - unzip datomic-pro-0.9.5350.zip
#    - sed -ie 's@license-key=@'"license-key=$DATOMIC_LICENSE_KEY"'@' datomic-pro-0.9.5350/config/samples/dev-transactor-template.properties
#    - datomic-pro-0.9.5350/bin/transactor config/samples/dev-transactor-template.properties :
#        background: true

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/7f6cadb429def50c94a2

deployment:
  update-projects:
    branch: master
    commands:
      - lein deploy
